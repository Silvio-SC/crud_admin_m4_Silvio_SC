import { userSchema, userSchemaCreate, userSchemaCreateResp, userSchemaLogin } from "./users.schema"
import { courseSchema, courseSchemaCreate } from "./courses.schema"
import { userCourseSchema, userCourseSchemaCreate } from "./user-courses.schema"

export { 
    userSchema, userSchemaCreate, userSchemaCreateResp, userSchemaLogin, 
    courseSchema, courseSchemaCreate,
    userCourseSchema, userCourseSchemaCreate 
}